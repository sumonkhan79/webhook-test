pipeline {
    agent any
    stages {
        stage('build') {
            steps {
            
                sh '''
                source /etc/profile
                python --version
                '''
            }
        }
    }
}
